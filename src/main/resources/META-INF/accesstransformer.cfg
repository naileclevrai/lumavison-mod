# LumaVision access transformers
# Needed so the camera capture can temporarily redirect Minecraft's main render target
# to an offscreen framebuffer while rendering a camera's view (see CameraViewCapture).
public-f net.minecraft.client.Minecraft f_91042_ # mainRenderTarget
